
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
              //echo
              echo "Build in progress"
                // Get some code from a GitHub repository
                git 'https://github.com/kennethonyenuloya/Selectapension.git'
            }           
        }
    }
}
